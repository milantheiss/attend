services:
  mongodb:
    image: mongo:7
    container_name: attend-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: attend
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
